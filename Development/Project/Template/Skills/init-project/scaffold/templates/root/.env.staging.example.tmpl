# staging — 部署平台填入實際值,**禁**沿用 development 預設
APP_ENV=staging
{{#if include_database}}POSTGRES_VERSION={{postgres_version}}
DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@HOST:{{postgres_port}}/{{project_name_underscore}}
{{/if}}JWT_SECRET_KEY=__GENERATE_AT_DEPLOY__
CORS_ORIGINS=["https://staging.example.com"]
