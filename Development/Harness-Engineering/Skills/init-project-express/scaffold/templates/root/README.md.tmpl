# {{project_name}}

<一句話描述專案。>

## 系統需求

- Node {{node_version}}+
{{#if include_database}}- PostgreSQL {{postgres_version}}+(請依官方文件本機安裝並啟動)
{{/if}}
## 快速開始(本地開發)

### 一鍵啟動(推薦)

在 Claude Code 輸入 `/start-dev`,skill 會自動:
1. 確認當前是 scaffold 出來的專案目錄
2. 偵測 `:{{backend_port}}`(backend)、`:{{frontend_port}}`(frontend)是否已被舊 process 占用 → kill
3. 各自啟動 ts-node-dev / Next.js dev server
4. 印出網址

停止:跑 `/stop-dev`,或重跑 `/start-dev`(會自動 kill 舊的)。

### 首次跑前要做的準備

{{#if include_database}}1. **本機 PostgreSQL** 啟動,並依 `.env` 內 `DATABASE_URL` 的 user / password / db 建立 PG role 與 DB(預設 `{{postgres_user}}`):
    ```sql
    -- ⚠ 密碼以本專案 .env 裡 DATABASE_URL 的 password 為準(.env 已 .gitignore)
    CREATE USER {{postgres_user}} WITH PASSWORD '<把 .env DATABASE_URL 內的 password 貼這>';
    CREATE DATABASE {{project_name_underscore}} OWNER {{postgres_user}};
    ```
2. `.env` 已由 `/init-project-express` 自動產出(內含 `DATABASE_URL`);若沒 `.env`,`cp .env.development.example .env` 並手動編輯
3. `cd backend && npm ci && npx prisma migrate dev --name init && cd ..`
4. `cd frontend && cp .env.local.example .env.local && npm ci && cd ..`
5. 跑 `/start-dev`(上一段)
{{/if}}{{#if !include_database}}1. `cp .env.development.example .env`,編輯 `.env` 填入機密
2. `cd backend && npm ci && cd ..`
3. `cd frontend && cp .env.local.example .env.local && npm ci && cd ..`
4. 跑 `/start-dev`(上一段)
{{/if}}

### 驗證

- Backend Health :http://localhost:{{backend_port}}/api/{{api_version}}/health{{#if include_database}} → `{data: {db: "ok"}}`{{/if}}
- Frontend       :http://localhost:{{frontend_port}}

### 手動啟動(不用 skill)

若不在 Claude Code 環境,或要拆兩個 terminal 各自看 log:

```bash
# Terminal 1:後端
cd backend && npm run dev

# Terminal 2:前端
cd frontend && npm run dev
```

---

## 技術棧

- 前端:Next.js {{next_version}} + TypeScript {{typescript_version}} + Redux Toolkit + Tailwind v4
- 後端:Express {{express_version}} + TypeScript + Zod{{#if include_database}} + Prisma {{prisma_version}}{{/if}}
{{#if include_database}}- 資料庫:PostgreSQL {{postgres_version}}
{{/if}}
## 工作流程

- 主分支 `main`,功能分支從 `main` 切出
- Commit message 繁體中文 `<類型>: <描述>`(類型:Add / Modify / Fix / Refactor / Docs)

## 部署

部署方案由本專案維護者依採用平台另行設計。本骨架為基本可跑的 Next.js + Express{{#if include_database}} + PostgreSQL{{/if}} 環境。
